# 小綿羊上樓梯 / Sheep Stair Climb 製作說明

## 1. 專案目標

製作一款 9:16 直式 HTML5 網頁遊戲，可於桌機與手機瀏覽器直接遊玩。遊戲玩法靈感來自「角色持續移動、蓄力跳躍、踩平台向上推進」類型，但所有角色、介面、素材、名稱與視覺表現皆使用本專案原創素材，不複製任何既有遊戲的美術、名稱、音效或商標。

遊戲暫定名稱為「小綿羊上樓梯」，英文代號為 `Sheep Stair Climb`。

網頁瀏覽器抬頭需設定為：

- `小綿羊上樓梯 │ 天品山莊出品 │ 天品集團股票代號9166`

網頁小圖示 favicon 使用：

- `PIC/Game assets/background/TPICON.png`

技術採純前端實作：

- 使用 `HTML`、`CSS`、`JavaScript`
- 不使用 Canvas API
- 不使用後端
- 不使用大型框架
- 不依賴外部 CDN
- 開啟 `index.html` 即可遊玩

預計專案檔案至少包含：

- `index.html`
- `style.css`
- `game.js`
- `README.md`
- `PIC/Game assets/` 內既有遊戲素材
- `PIC/Game illustration/` 內既有示意圖

## 2. 畫面比例與裝置支援

遊戲主要畫面比例固定為 9:16 直式，邏輯尺寸可採 `360 x 640` 作為定位基準，實際顯示依裝置大小等比縮放。

手機支援需求：

- 手機直式畫面正常顯示
- 桌機瀏覽時遊戲畫面置中，不可過寬
- 手機 Safari、Chrome、Firefox 可正常遊玩
- 支援安全區域 `safe-area-inset`
- 禁止頁面上下滑動影響遊戲
- 避免手機誤觸縮放或滾動
- 處理 `pointerdown`、`pointerup`、`touchstart`、`touchmove`、`touchend`
- 未來可移植成手機 App

## 3. 素材與示意圖

示意圖位置：

- `PIC/Game illustration/bgi_01.png`：遊戲主畫面示意
- `PIC/Game illustration/bgi_02.png`：遊戲中畫面示意
- `PIC/Game illustration/bgi_03.png`：遊戲結束且進入排行榜的畫面示意
- `PIC/Game illustration/bgi_04.png`：遊戲結束但未進入排行榜的畫面示意

素材位置：

- `PIC/Game assets/background/`
- `PIC/Game assets/Button/`
- `PIC/Game assets/cloud/`
- `PIC/Game assets/loor/`
- `PIC/Game assets/power/`
- `PIC/Game assets/role/`

素材使用原則：

- 角色素材只有面向左版本，角色向右移動時使用 CSS 水平反轉呈現。
- 遊戲以 DOM 圖層與 CSS 動畫組成，不使用 Canvas。
- 畫面布置需優先參照四張示意圖。
- 實作時不可只將素材大致放入畫面，需依 `bgi_01.png`、`bgi_02.png`、`bgi_03.png`、`bgi_04.png` 的構圖、比例、上下留白與主要物件位置進行對版。
- 按鈕需有未按下與按下狀態圖，點擊或觸控時切換呈現按下感。

需注意的檔名對應：

- 集氣量表第 6 級目前素材檔名為 `PPOWER_06.png`，其餘為 `POWER_00.png` 至 `POWER_10.png`。實作時需兼容此檔名，或後續將檔案改名為 `POWER_06.png`。
- 需求文字中的 `UP_02.png` 應對應現有素材 `role/JUP_02.png`。
- 需求文字中的 `GO02.png` 應對應現有素材 `role/GO_02.png`。

## 4. 音效規格

音效素材位置：

- `sound/Button.mp3`：一般按鈕點擊音效。
- `sound/fall-01.MP3`：角色掉出畫面時播放。
- `sound/fall-02.MP3`：掉落平台開始下落時播放。
- `sound/get.MP3`：角色取得水晶球時播放。
- `sound/START.mp3`：開始與重新開始遊戲音效。
- `sound/power.MP3`：開始集氣音效。
- `sound/Jump.mp3`：放開集氣起跳音效。
- `sound/landing.mp3`：落地於地面或平台音效。
- `sound/gameover -01.mp3`：Game Over 音效。
- `sound/Leaderboard.MP3`：進入前五名、顯示排行榜輸入提示音效。
- `sound/moodmode-retro-game-music-245230.MP3`：背景音樂，低音量循環播放。

音效播放規則：

- 不使用外部 CDN，不新增後端。
- 手機瀏覽器需等待第一次點擊、觸控或按鍵後解鎖音訊，再開始播放音效與背景音樂。
- 頁面載入時可先嘗試以靜音方式播放或預載背景音樂；第一次點擊、觸控或按鍵後需解除靜音並正常播放。若瀏覽器阻擋靜音自動播放，也需在第一次互動時補啟動背景音樂。
- 背景音樂音量需低於操作音效，不可干擾跳躍與落地回饋。
- 同一次觸控可能同時觸發 pointer 與 touch 事件，按鈕音效需做短時間防重複。

## 5. 遊戲狀態

遊戲狀態至少包含：

- `menu`：主畫面
- `intro`：主畫面素材依序浮現的開場動畫
- `playing`：遊戲中
- `paused`：暫停
- `gameover`：遊戲結束
- `admin`：排行榜後台管理介面

狀態切換：

- 開啟頁面後進入主畫面開場動畫。
- 點擊 `START-01.png` 後按鈕切換為 `START-02.png`，再進入遊戲畫面。
- 遊戲中點擊右上暫停／繼續按鈕切換 `playing` 與 `paused`。
- 角色掉出畫面底部後進入 `gameover`。
- Game Over 可選擇重新開始、回主畫面或前往外部網站。

## 6. 主畫面規格

開場 splash 畫面依序出現以下圖層與元素：

1. 遊戲載入後立刻顯示底圖 `background/bg_00.png`。
2. 開場前 0.5 秒畫面只能顯示 `background/bg_00.png`，不可提前出現其他素材。
3. 0.5 秒後以淡入效果顯示 `background/bg_06.png`。
4. `background/bg_06.png` 必須位於畫面正中心。
5. `background/bg_06.png` 淡入完成後，該開場畫面再停留 1.5 秒。
6. 停留結束後自動進入正式主畫面。

正式主畫面依序出現以下圖層與元素：

1. 顯示主畫面背景 `background/bg_01.png`
2. 顯示底部版權條 `background/bg_02.png`
3. 出現遊戲 LOGO，由三張圖層疊組成：
   - `background/LOGO_01.png`
   - `background/LOGO_02.png`
   - `background/LOGO_03.png`
4. 首頁主視覺圖層順序由下到上必須是：`Leaderboard_01.png`、`LOGO_01.png`、`LOGO_02.png`、`LOGO_03.png`。
5. `LOGO_01.png`、`LOGO_02.png`、`LOGO_03.png` 的位置需與 `bgi_01.png` 示意一致；其中 `LOGO_03.png` 使用全畫面定位以對齊示意圖主標題，`LOGO_01.png` 彩虹層位於上方偏左，`LOGO_02.png` 角色吉祥物位於上方偏右。
6. `LOGO_01.png`、`LOGO_02.png`、`LOGO_03.png` 需作為同一組整體向下位移 0.4 公分，實作以約 15px 對應。
7. LOGO 彩虹與吉祥物層需做輕微飄動位移，營造動感，但不可偏離示意圖主構圖。
8. LOGO 與吉祥物需位於排行榜板上層，主標題不可被排行榜板遮住；角色吉祥物不可貼邊或被右側裁切。
9. 出現排行榜紀錄表 `background/Leaderboard_01.png`，位置與大小需接近更新後 `bgi_01.png` 中央下方黃色排行板，不可過寬或壓到 START。
10. 讀取排行榜資料並依序呈現前五名資訊；五列資料不使用等距行距，而是每一列各自對應內建 `1st`、`2nd`、`3rd`、`4th`、`5th` 標籤中心高度，讓第 1 筆資料對應 `1st`、第 2 筆資料對應 `2nd`，依此類推。資料文字需位於排名標籤右側，字體大小與水平位置需對齊示意圖。
11. 五名資訊出現時可加入發亮效果。
12. 出現開始按鈕 `Button/START-01.png`，位置需接近 `bgi_01.png` 底部中央。
13. 排行榜板底部與 START 按鈕之間需保留更新後 `bgi_01.png` 所示的明顯草地空隙，不可互相重疊或視覺上貼太近。
14. `background/bg_02.png` 是底部版權條，需貼齊畫面底部，不可放在畫面上方。
15. 點擊或觸碰開始按鈕時切換為 `Button/START-02.png`，短暫呈現按下感後進入遊戲。
16. 首頁物件動畫若因瀏覽器時序未完成，需有保險機制讓 LOGO、排行榜與 START 在動畫結束後保持可見，避免首頁只剩背景。
17. 主畫面不顯示「尚無個人紀錄」字樣；排行榜五列的 `---` 位置視為前五名資料欄位，未有資料時可暫以 `---` 佔位。

主畫面需顯示：

- 遊戲標題或 LOGO
- 前五名排行榜
- 玩家個人目前最高紀錄資料需保存，但主畫面不顯示「尚無個人紀錄」字樣
- `START` 開始按鈕

## 7. 遊戲中畫面規格

遊戲中畫面圖層由後到前如下：

1. 背景 `background/bg_03.png`
2. 雲朵 `cloud/cloud_01.png` 至 `cloud/cloud_05.png`
3. 前景 `background/bg_04.png`，初始需貼齊畫面底部；後續可隨鏡頭位移作為前景層使用
4. 起始地面 `background/bg_Bwall.png`，初始需貼齊畫面底部；後續需以遊戲世界座標隨鏡頭位移，不可同時使用 `bottom` 與世界座標 transform 造成定位偏移
5. 平台 `background/platform_01.png`、`background/platform_02.png`
6. 水晶球 `background/TPB-01.png`、`background/TPB-02.png`，位於平台之上、角色之下
7. 角色 `role/` 內對應動作素材
8. 邊框牆面 `background/bg_LRUwall.png`
9. 上方 UI：集氣量表、樓層數、暫停／繼續按鈕

上方 UI：

- 左側：集氣量表，使用 `power/POWER_00.png` 至 `power/POWER_10.png`，初始為 0，位於 UI 層最下方。
- 中間：累積樓層數，使用 `loor/0.png` 至 `loor/9.png` 與 `loor/F.png` 組成，初始顯示 `000F`，位於 power 之上。
- 右側：暫停／繼續切換按鈕，使用 `Button/STOP-01.png` 與 `Button/STOP-02.png`，位於 HUD 最上層。
- 暫停時不顯示 `PAUSED` 文字視窗；改為在遊戲畫面上覆蓋 70% 透明黑色遮罩。暫停／繼續按鈕 `Button/STOP-01.png` / `Button/STOP-02.png` 必須與遮罩同層並位於遮罩之後，圖層高於遮罩，確保玩家知道可按右上按鈕繼續遊戲。

遊戲畫面本機自測：

- 可使用 `index.html?screen=game` 直接進入遊戲畫面，用於檢查 `bgi_02.png` 對位與初始元件布局。
- 可使用 `index.html?screen=game&autojump=1` 直接進入遊戲畫面並自動執行一次「集氣 0.35 秒後放開跳躍」，用於檢查跳躍姿態與離地效果；一般遊玩不會觸發此測試流程。
- 可使用 `index.html?screen=game&qa=platformCharge` 將角色放到平台中心並自動按住集氣，用於檢查平台支撐區是否能立即集氣，以及集氣量表是否依實際按住時間累積；一般遊玩不會觸發此測試流程。
- 遊戲第一屏需維持 `bgi_02.png` 的整體視覺比例與素材層級，但平台位置改用正式遊戲的隨機生成規則，不再使用固定座標排列。
- 每次 START / RESTART 都會重新產生平台分布；同一局已生成的平台需保持穩定，不可因鏡頭回看而跳動。
- 第一屏角色尺寸需比一般遊戲角色素材縮小，站在底部地面上方，位置接近 `bgi_02.png` 底部中央偏左。
- 第一屏雲朵需使用 `cloud/` 資料夾素材隨機分布，大小與位置隨機，維持先前的天空層次感。

雲朵規則：

- 雲朵從 `cloud_01.png` 至 `cloud_05.png` 隨機選用。
- 雲朵位置、大小隨機生成。
- 雲朵需做輕微飄動位移。
- 角色向上推進、鏡頭上移時，雲朵會相對往下移動，離開畫面底部後消失。
- 遊戲進行中需依時間持續隨機補充新的雲朵；即使鏡頭暫時沒有上移，背景也會陸續出現新雲朵。
- 雲朵需維持最少背景層次與最高數量上限，避免畫面空掉或過度擁擠。

平台規則：

- `platform_01.png`：固定位置平台。
- `platform_02.png`：移動平台，隨機左右或上下緩慢移動。
- `platform_03.png`：掉落平台，15F 之後才會零星出現，用來替換原本的固定平台，不額外增加平台總數。
- 每 10 層樓需出現一顆水晶球，水晶球放在該樓層某一個固定平台上；承載水晶球的平台不可是移動平台或掉落平台。
- 0F 初始區第一個固定平台上需出現一顆水晶球；此為正式規則，不再只是測試用。
- 水晶球使用 `background/TPB-01.png` 與 `background/TPB-02.png` 略快交替顯示，呈現閃爍效果。
- 水晶球需有上下輕微浮動效果，且浮動位置需同步影響取得判定範圍。
- 角色碰到水晶球後，水晶球消失，播放 `sound/get.MP3`，並獲得 3 次空中「兩段跳」能力。
- 移動平台最大移動幅度約為一個 `platform_02.png` 寬度。
- 平台生成以「固定垂直節奏搭配微量錯位」進行：每 190px 高度必須生成 1 個必要平台，確保玩家每次往上跳都有明確目標；平台實際 Y 軸可有小幅隨機錯位，避免畫面看起來像規律階梯。
- 為降低畫面密集感但保留足夠落點，每個 190px 必要平台區間中需包含 1 個必要平台與 2 個輔助平台，因此實際平台垂直節奏約為每 63.3px 一個平台；每 3 個平台中第 1 個為必要平台，其餘 2 個為輔助平台。
- 平台數量需避免回到過度密集的每 190px 4 個平台；若後續覺得空，可優先調整平台橫向分布與角色速度，不直接恢復高密度。
- 640px 高度畫面中通常需可見約 10-11 個平台，讓玩家仍有選擇空間但不會過於擁擠。
- 起始畫面平台也需使用同一套約 63.3px 輔助節奏與 190px 必要平台規則，不再使用固定平台排列。
- 橫向位置以原素材寬度 1080px 的 216px 為一區，換算到 360px 遊戲畫面後分成 5 個區域。
- 平台分布需以每 5 個平台為一組使用 5 區域洗牌規則，讓 5 個區域都會輪到，避免連續平台集中在同一側。
- 每次 START / RESTART 都需重新產生本局的 5 區域洗牌順序與起始靠牆方向；同一局內已生成的平台位置保持穩定，不可因鏡頭回看而跳動。
- 每 5 個平台中必須有 1 個指定平台靠任意一側牆，左右側依本局起始方向交替，增加路線變化但維持固定節奏；其他非指定平台優先分布於中間區域，避免連續靠牆。
- 連續平台不可出現過於一致的 X 軸距離或連續兩個都剛好靠牆；產生平台時需重抽候選位置，讓路徑有變化但仍可玩。
- 平台不可讓玩家一眼看出單一明確曲線路徑；每個畫面平台組中需加入更多分岔平台，分岔平台需和同高度主平台保持明顯水平距離，讓玩家有左右兩側替代落點。
- 分岔平台可和主平台高度接近，但需避免和主平台水平重疊；分岔位置以中間區域為主，並以低機率優先產生靠牆分岔，讓牆邊替代落點偶爾出現但不成為主路線。分岔平台使用現有固定平台或移動平台素材，不新增外部素材。
- `platform_02.png` 移動平台需分為左右移動與上下移動兩種；左右移動平台與上下移動平台比例約為 70% / 30%。
- 上下移動平台的垂直移動幅度需比原設定增加 100%；左右移動平台幅度不受此調整影響。
- `platform_02.png` 進行左右反覆移動時，視覺上可以移出牆面外側；這不改變角色本身的牆面限制，角色仍依左右 1/3 判定線碰牆折返。
- 15F 後，約 20% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 35F 後，約 25% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 45F 後，約 30% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 55F 後，約 50% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 65F 後，約 60% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 75F 後，約 70% 原本會生成為 `platform_01.png` 的固定平台可被替換為 `platform_03.png` 掉落平台。
- 不可把掉落平台作為額外平台新增，避免改變整體平台密度。
- 玩家踩上 `platform_03.png` 後，平台需先維持約 0.5 秒，再開始輕微左右搖晃並向下掉落；平台掉出畫面前仍算有效支撐平台，玩家可站在上面、可再次起跳。
- 樓層越高，平台中移動平台比例、移動速度與移動距離依下表增加，難度逐漸提高。
- 移動平台數量以「每個畫面可見平台組」為單位控制；目前一個畫面約 10-11 個平台，因此以每 12 個平台為一組，在該組內依樓層區間配置指定數量的 `platform_02.png`。
- 例如 1-15F 並不是整個 1-15F 只出現 1 個移動平台，而是 1-15F 的每一個畫面平台組中都有 1 個移動平台；16-30F 的每一個畫面平台組中都有 2 個移動平台。
- 移動平台需在同一畫面平台組中分散配置，不可集中連續出現，除非 261F 之後全數平台皆為移動平台。
- 所有移動平台的實際速度在難度倍率之外，再整體乘以 1.5，讓平台移動比原基礎設定快 50%。
- 移動速度加快 0.5 倍代表基礎速度乘以 1.5；加快 1 倍代表乘以 2；加快 1.5 倍代表乘以 2.5。
- 移動距離增長 0.5 倍代表基礎距離乘以 1.5；增長 1 倍代表乘以 2；增長 2 倍代表乘以 3。

平台難度區間：

| 樓層 | 移動平台 `platform_02.png` 數量 | 移動速度 | 移動距離 |
| --- | ---: | --- | --- |
| 1-15 | 1 個 | 基礎 | 基礎 |
| 16-30 | 2 個 | 基礎 | 基礎 |
| 31-45 | 3 個 | 基礎 | 基礎 |
| 46-60 | 4 個 | 基礎 | 基礎 |
| 61-75 | 2 個 | 加快 0.5 倍 | 基礎 |
| 76-100 | 3 個 | 加快 0.5 倍 | 基礎 |
| 101-115 | 4 個 | 加快 0.5 倍 | 基礎 |
| 116-130 | 2 個 | 加快 1 倍 | 增長 0.5 倍 |
| 131-145 | 3 個 | 加快 1 倍 | 增長 0.5 倍 |
| 146-160 | 4 個 | 加快 1 倍 | 增長 0.5 倍 |
| 161-175 | 2 個 | 加快 1 倍 | 增長 1 倍 |
| 176-200 | 3 個 | 加快 1 倍 | 增長 1 倍 |
| 201-215 | 4 個 | 加快 1 倍 | 增長 1 倍 |
| 216-230 | 2 個 | 加快 1.5 倍 | 增長 1 倍 |
| 231-245 | 3 個 | 加快 1.5 倍 | 增長 1 倍 |
| 246-260 | 4 個 | 加快 1.5 倍 | 增長 1 倍 |
| 261-300 | 全數平台 | 加快 1.5 倍 | 增長 0.5 倍 |
| 301-999 | 全數平台 | 加快 1.5 倍 | 增長 1 倍 |

- 平台需視為地面：角色落到平台上後會 grounded，可在平台上左右移動與集氣。
- 角色每一幀都需檢查腳下平台是否仍有支撐；平台支撐與平台集氣改以角色素材左右 1/3 判定線作為標準，不可以整張角色圖寬度或單一中心點判斷，避免角色看起來站在平台邊緣騰空。
- 若角色水平移動到接近平台邊緣且左右 1/3 判定線形成的支撐區已離開平台有效範圍，需取消 grounded 並受重力向下掉落。
- 若角色正在平台上集氣但移出平台範圍，需立即取消集氣、量表歸零並開始下墜。
- 平台集氣判定需與掉落判定分開：集氣允許使用較寬的腳底附近範圍，只要角色視覺上仍被平台承托，按下就能立即集氣；掉落仍使用較窄支撐區避免邊緣騰空。
- 集氣中也需使用平台集氣支撐範圍維持狀態；不可按下後下一幀又被較窄的掉落支撐判定立刻取消，只有離開集氣支撐範圍時才中斷集氣。
- 平台集氣維持範圍可比平台本體左右各多保留少量容差，以避免角色視覺仍在平台上時集氣被過早取消；此容差不影響一般掉落判定。
- 地面或平台等支撐元件若隨鏡頭下移至畫面底部之外，即判定失去支撐效果；不可再提供站立、集氣或落地碰撞。

## 8. 角色與操作規格

角色初始腳底需位於 `bg_Bwall.png` 的上緣，並於底部地面上方反覆左右移動，接觸牆面後折返。

角色素材：

- 待機或一般移動：`role/GO_01.png`、`role/GO_02.png`
- 一般集氣效果：按住滑鼠或觸控時快速交替 `role/GOS_01.png`、`role/GOS_02.png`，模擬閃爍集氣效果。
- 角色取得水晶球且仍有兩段跳次數時，集氣行走／集氣狀態圖像改用 `role/GOS_03.png`、`role/GOS_04.png` 交替，呈現行走畫面。
- 跳躍效果：`role/JUP_01.png`、`role/JUP_02.png`
- 遊戲中主角色圖檔為正方形素材，顯示時需以固定高度與寬度呈現，並以角色圖層底部作為腳底基準，確保腳底貼齊 `bg_Bwall.png` 上緣。

角色方向：

- 素材預設為面向左。
- 向左移動時正常顯示。
- 向右移動時使用 CSS `transform: scaleX(-1)` 反轉。

操作方式：

- 桌機：滑鼠左鍵按住集氣，放開跳躍。
- 桌機：也可按住空白鍵集氣，放開空白鍵跳躍；空白鍵只在遊戲進行中作用，不可影響姓名輸入欄與後台輸入欄。
- 桌機：也可按 `Enter` 暫停或繼續遊戲；`Enter` 只在遊戲進行中或暫停中作用，不可影響姓名輸入欄與後台輸入欄。
- 手機：觸碰按住集氣，放開跳躍。
- 遊戲中點擊或觸碰遊戲視窗內除了暫停／繼續按鈕之外的任何地方，都視為開始集氣；背景、牆面、角色、平台、HUD 空白區皆需可觸發。
- 開始集氣後需使用指標捕捉或全視窗放開監聽，確保手指或滑鼠離開原按下目標後放開，仍會觸發跳躍。
- 集氣開始必須是即時的：按下當下立即切換 `GOS_01.png` 並開始計時；若同一觸控同時觸發 pointer 與 touch 事件，不可重設集氣開始時間。
- 按下當下需立即重新確認角色腳底是否在有效支撐區；只要在 `bg_Bwall.png` 地面或平台有效支撐區內，就可以立刻開始集氣。
- 平台上的集氣判定需允許些微垂直容差，角色腳底在平台支撐區附近時，按下當下需立刻同步為可集氣狀態，不可等下一個物理更新。
- 若角色不在任何有效支撐區內，按住不可開始集氣；若集氣中離開支撐區，需立刻中斷集氣。
- 放開必須立即中止集氣並跳躍；即使是極短按放，也至少以 1 級跳躍處理。
- 輸入需在 capture 階段處理滑鼠與觸控的按下／放開，避免事件冒泡延遲造成集氣量表不同步或放開後延遲中止。
- 暫停狀態下不可集氣、不可跳躍，角色與平台暫停更新。
- 角色水平來回移動速度設定為 78。

集氣規則：

- 集氣量表分為 0 至 10 級。
- 從 0 級累積到 10 級總時長為 1 秒。
- 集氣量表需每幀依實際按住時間 `performance.now() - chargeStartedAt` 重新換算，不可依賴累加幀數或舊狀態；第一輪需顯示 0 至 10 級的離散變化。
- 畫面左上方 power 圖像本身也需每幀同步切換 `POWER_00.png` 至 `POWER_10.png`，必須在按住期間看得到 0-10 級逐步累積效果，不可只在放開時顯示當下累積程度。
- 到達 10 級後回到 1 級，持續循環，直到放開。
- 角色放開起跳後可短暫保留當下集氣等級圖像；但角色落到地面或平台後，集氣量表圖像必須立即回到 `POWER_00.png`。
- 第 0 級為無集氣。
- 放開按鍵或觸控時需即時計算當下集氣等級，不可只依賴畫面更新迴圈，避免短按或手機觸控放開時仍停在 0 級而無法跳躍。
- 第 1 級跳躍高度約為畫面一半高度的十分之一。
- 第 10 級跳躍高度約可達畫面一半高度。
- 集氣中角色快速交替顯示 `GOS_01.png`、`GOS_02.png`，模擬閃爍集氣效果。
- 若角色已取得水晶球能力且仍有兩段跳次數，角色在半空中也可以按住畫面開始集氣；空中集氣期間角色仍受重力影響。
- 若角色空中集氣時落到平台，且玩家仍持續按住畫面，落地後需立即切換為地面集氣狀態，不需要放開再重新按住。

跳躍規則：

- 放開滑鼠或手指後立即跳躍。
- 跳躍瞬間角色顯示 `JUP_01.png`，接著顯示 `JUP_02.png`，再回到 `JUP_01.png`，模擬跳躍與落地動作。
- 跳躍影格序列播放一次後停在最後一張 `JUP_01.png`，直到角色落地再切回一般移動或集氣狀態。
- 起跳瞬間需先讓角色離開地面判定線，並給予極短起跳保護時間，避免放開後立刻被 `bg_Bwall.png` 地面碰撞判定抓回一般狀態。
- 跳躍高度依放開時的集氣量表級數決定。
- 跳躍時角色仍持續水平移動；空中水平位移需比地面移動距離再加長 30%，但不改變跳躍高度與重力。
- 跳躍、下落或水平移動時，角色以素材左 1/3 判定線碰到左牆、右 1/3 判定線碰到右牆作為折返標準。
- 角色受重力影響下落。
- 持有水晶球能力時，角色在半空中集氣並放開後會立即再次向上跳；只有「空中集氣完成後放開並成功觸發跳躍」才消耗 1 次兩段跳次數。空中按住但未跳起、取消、落地或 Game Over 不計次。每顆水晶球提供 3 次可用次數。
- 只有角色正在下落時，才判定是否踩到平台。
- 已經 grounded 站在地面或平台上的角色，不可再次觸發平台／地面落地碰撞；否則會反覆呼叫落地流程並取消正在進行的集氣。

## 9. 樓層、鏡頭與 Game Over

一開始底部為地面 `bg_Bwall.png`，角色可在地面上移動與跳躍。

當角色成功往上落地於一定高度的平台後：

- 落到平台本身不直接計算樓層。
- 若角色高度超過畫面上半部，畫面鏡頭往上推進。
- 只有螢幕鏡頭實際上移時才累積樓層進度，但不以「上移一次」直接加 1 樓。
- 鏡頭實際累積向上推升滿 190px 時才增加 1 樓，並更新樓層數顯示。
- 若玩家只在畫面一半附近反覆小跳，造成鏡頭微量上移但未累積滿 190px，不可增加樓層。
- Game Over 最終樓層以鏡頭上移累計值為準，不可再用平台高度或角色最高高度重新換算。
- 雲朵、起始地面 `bg_Bwall.png`、前景 `bg_04.png` 會隨鏡頭上移而相對往下移動。
- 支撐平台或起始地面一旦下移出畫面底部外，角色若仍依附其上需立即失去支撐並開始下墜。

當起始地面 `bg_Bwall.png` 離開畫面底部後：

- 底部開始視為深谷。
- 角色掉出畫面底部之外即遊戲結束。

Game Over 判定：

- 角色掉出底部深谷畫面外時進入遊戲結束。
- 角色落出畫面外觸發 Game Over 後，遊戲畫面需先維持 0.7 秒，再切換到 Game Over 畫面。
- 立即計算並呈現最終累計樓層數。
- 若本次成績進入資料庫前五名，顯示排行榜輸入對話框，版面參照 `bgi_03.png`。
- 若本次成績未進入資料庫前五名，顯示一般結果欄窗，版面參照 `bgi_04.png`。

## 10. 遊戲結束畫面

Game Over 畫面圖層由後到前如下：

1. `background/bg_05.png`
2. `background/bg_LRUwall.png`
3. `background/bg_Bwall.png`
4. `background/gameover logo.png`，尺寸與位置需接近 `bgi_03.png` 上方大標題，不可過大或貼邊；進入 Game Over 畫面時需淡入，並由下方些微上移至目前位置
5. 底部休息角色，使用 `role/STAY_01.png` 與 `role/STAY_02.png` 交替，並於底部小幅來回移動
6. 依成績狀態顯示進榜輸入欄窗或一般結果欄窗
7. 三個並排按鈕

三個按鈕：

- 三個按鈕需依 `bgi_03.png` 橫向並排於畫面下半部，保持左右留白與按鈕間距。
- 左側：重新開始遊戲
  - 未按下：`Button/RESTART-01.png`
  - 按下：`Button/RESTART-02.png`
- 中間：前往外部網站
  - 未按下：`Button/GOTP-01.png`
  - 按下：`Button/GOTP-02.png`
  - 點擊後開新分頁，並從下列三個網址隨機選一個：
    - `https://www.tienpin.com.tw`
    - `https://www.youtube.com/@%E5%A4%A9%E5%93%81%E5%B1%B1%E8%8E%8A%E5%9F%BA%E7%9D%A3%E5%BE%92%E5%A2%93%E5%9C%92/videos`
    - `https://sites.google.com/view/tienpingroupweb/%E9%A6%96%E9%A0%81`
- 右側：回主畫面
  - 未按下：`Button/MENU-01.png`
  - 按下：`Button/MENU-02.png`

若本次成績進入前五名：

- 疊上 `background/Leaderboard_02.png`
- `Leaderboard_02.png` 需放在 `bgi_03.png` 示意的中段黃色輸入板位置。
- 若素材更新為新的 `Leaderboard_02.png`，頁面需繼續使用 `PIC/Game assets/background/Leaderboard_02.png` 原路徑讀取。
- 不另外疊上「恭喜你的樓層積分獲得前五名」與「請輸入你的名字留念(限六個字)」文字；這些提示若需要呈現，需由 `Leaderboard_02.png` 圖面本身承載。
- 本次樓層數需使用 `PIC/Game assets/loor/` 內的 `0.png` 至 `9.png` 與 `F.png` 圖檔組合顯示，尺寸與位置需參照 `bgi_03.png`。
- 目前進榜樓層數圖像已依畫面微調：較前一版縮小 10%，並下移 5px。
- 疊上送出按鈕：
  - 未按下：`Button/GO-01.png`
  - 按下：`Button/GO-02.png`
- 送出按鈕尺寸與位置需依 `bgi_03.png` 示意放在輸入欄窗下緣中央；目前送出按鈕已較前一版縮小 10%。
- `Leaderboard_02.png` 中兩條橫線之間需放置文字輸入欄位，欄位底色需比欄窗底色略淺，欄位需為圓角。
- 目前進榜姓名輸入欄已依畫面微調，較前一版下移 1px；送出按鈕較前一版下移 4px。
- 進入前五名時需自動聚焦文字輸入欄，顯示閃爍輸入游標，提示玩家可直接輸入；輸入欄外框不做閃爍效果。
- 玩家可輸入姓名，最多六個中文字。
- 只有姓名欄有輸入文字並點擊 `GO` 時，才寫入排行榜資料。
- 若姓名欄未輸入文字就點擊 `GO`，視為放棄留名，不寫入排行榜。
- 若玩家點擊 `RESTART` 重玩或 `MENU` 回主畫面，無論姓名欄是否已有文字，皆視為放棄留名，不寫入排行榜。

若本次成績未進入前五名：

- 疊上 `background/Leaderboard_03.png`
- `Leaderboard_03.png` 需放在 `bgi_04.png` 示意的中段黃色結果板位置。
- 不另外疊上「本次挑戰的樓層積分／排行」與「你的最高紀錄」文字；這些標示由 `Leaderboard_03.png` 圖面本身承載。
- 本次樓層數與最高紀錄樓層數需使用 `PIC/Game assets/loor/` 內的 `0.png` 至 `9.png` 與 `F.png` 圖檔組合顯示，尺寸與位置需參照 `bgi_04.png`。
- 目前未進榜欄窗中的兩組樓層數已依畫面微調，整體較原始版上移 10px。
- 未進榜時不顯示姓名輸入欄，也不顯示 `GO` 送出按鈕。

## 11. 紀錄、排行榜與後台管理

儲存方式：

- 純前端版本使用 `localStorage`。
- 目前資料只保存在同一裝置、同一瀏覽器。
- 若未來需要多人共用線上排行榜，需新增後端或雲端資料庫。
- 遊戲確認完成後，上線規劃為：遊戲本體檔案上傳至 GitHub，玩家資料、排行榜、最高紀錄與遊玩次數改存 Supabase。

排行榜規格：

- 主畫面顯示前五名。
- Game Over 後若成績進入前五名，顯示輸入姓名對話框。
- 排行榜依樓層數由高到低排序。
- 只在主畫面顯示前五名，但資料庫需保留所有玩家紀錄。

玩家資料需包含：

- 玩家姓名
- 個人最高樓層
- 個人最高紀錄時間，含年／月／日／時／分
- 個人遊玩次數

全體統計需包含：

- 目前有多少人玩過遊戲
- 所有玩家資料列表
- 所有玩家最高紀錄

後台管理介面需求：

- 可查看所有玩家名字。
- 可查看每位玩家的個人最高紀錄，含年月日時分與樓層。
- 可查看每位玩家玩過幾次。
- 可查看目前總共有多少人玩過遊戲。
- 可修改玩家名稱。
- 可修改樓層數。
- 可修改紀錄時間。
- 可刪除玩家或排行榜項目。
- 純前端版本的後台先管理同一瀏覽器 `localStorage` 內資料。

後台入口建議：

- 不放在主要遊戲按鈕中，避免一般玩家誤觸。
- 可使用網址參數 `?admin=1` 或鍵盤快捷鍵進入。
- 進入後台後提供回主畫面功能。

## 12. README 內容

`README.md` 需包含：

- 專案介紹
- 如何執行
- 操作方式
- 素材資料夾說明
- 網頁抬頭與 favicon 說明
- 手機支援說明
- 排行榜與 localStorage 說明
- 後台管理方式
- 後續可擴充功能
- 驗收條件

## 13. 驗收條件

完成後需確認：

- 桌機瀏覽器可直接開啟 `index.html` 遊玩。
- 瀏覽器分頁標題顯示 `小綿羊上樓梯 │ 天品山莊出品 │ 天品集團股票代號9166`。
- 瀏覽器分頁小圖示使用 `TPICON.png`。
- 手機直式畫面可正常遊玩。
- 畫面維持 9:16 比例。
- 觸控操作順暢。
- 頁面不會因手機滑動而跑版。
- 開場 splash 前 0.5 秒只能顯示 `bg_00.png`，0.5 秒後才於正中心淡入顯示 `bg_06.png`，淡入完成後再停留 1.5 秒才進入正式主畫面。
- 主畫面依序播放指定素材動畫。
- 首頁畫面的物件大小、位置與圖層順序需參照 `bgi_01.png`，LOGO 不被排行榜遮住、吉祥物不被裁切、排行榜與 START 不重疊。
- LOGO 三層有輕微飄動位移。
- 前五名排行榜可正確顯示。
- START 按鈕有按下狀態並能進入遊戲。
- 角色左右移動，向右時以反轉呈現。
- 按住畫面可集氣，放開可跳躍。
- 桌機空白鍵可按住集氣、放開跳躍。
- 桌機 Enter 鍵可暫停與繼續遊戲。
- 集氣量表 0 至 10 級循環運作。
- 角色可踩平台向上推進。
- 固定平台與移動平台都會生成。
- 雲朵可隨鏡頭移動並飄動。
- 樓層數會增加，初始顯示 `000F`。
- 地面離開畫面後，底部變成深谷。
- 角色掉出底部會進入 Game Over。
- 角色掉出底部後需先停留原遊戲畫面 0.7 秒，再切換至 Game Over 畫面。
- Game Over 畫面按示意圖與素材顯示。
- Game Over logo 需淡入並由下方些微上移至定位。
- Game Over 畫面底部角色會來回移動，並交替 `STAY_01.png` 與 `STAY_02.png` 呈現休息呼吸效果。
- 若進入前五名，可輸入姓名並送出。
- 最高紀錄與排行榜可保存。
- 後台可查看、修改、刪除排行榜與玩家資料。
- 外部網站按鈕可從三個指定網址中隨機選一個並開新分頁。
- 重新開始與回主畫面功能正常。
- Console 無明顯錯誤。

## 14. 線上部署載入規則

- GitHub Pages 版本需在進入開場畫面前，先預載並解碼第一輪會用到的重要圖片素材。
- 預載範圍包含開場、主畫面、遊戲畫面、Game Over、角色、平台、雲朵、水晶、按鈕、樓層數字與集氣量表。
- 預載期間以灰色底圖與白色進度讀取條覆蓋畫面，避免玩家在圖片尚未下載完成時看到圖層錯位或素材晚出現。
- 進度讀取條需依已完成預載的圖片數量同步推進。
- 預載完成後才移除遮罩，並從 `bg_00.png` 開始執行原本的開場流程。
- 若網路異常或單一素材載入過久，最多等待短時間後仍會進入遊戲，避免畫面永久停留。
